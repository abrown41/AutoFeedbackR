% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_func.R
\name{check_func}
\alias{check_func}
\title{Check if a function exists and returns expected outputs}
\usage{
check_func(func_name, inputs, expected_outputs)
}
\arguments{
\item{func_name}{Character string of the function name to check}

\item{inputs}{List of input parameter lists to provide to the function}

\item{expected_outputs}{List of expected outputs for each input}
}
\value{
Logical, TRUE if the function exists and returns correct values, FALSE otherwise
}
\description{
This function checks if a student-defined function exists and returns the expected output values
for provided inputs.
}
\examples{
# Define a correct function
my_add <- function(a, b) { a + b }

# Example 1: Successful check
check_func("my_add", 
           list(list(1, 2), list(3, -2)), 
           list(3, 1))  # Should return TRUE

# Example 2: Function doesn't exist
check_func("nonexistent_func", 
           list(list(1)), 
           list(1))  # Should return FALSE

# Example 3: Wrong output
wrong_add <- function(a, b) { a - b }
check_func("wrong_add", 
           list(list(5, 2)), 
           list(7))  # Should return FALSE 

# Example 4: Wrong number of arguments
bad_args <- function(a) { a + 1 }
check_func("bad_args", 
           list(list(1, 2)), 
           list(3))  # Should return FALSE
}
